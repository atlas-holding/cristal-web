apiVersion: apps/v1
kind: Deployment
metadata:
  name: cristal-web
  labels:
    app: cristal-web
    dxp.io/managed: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cristal-web
  template:
    metadata:
      labels:
        app: cristal-web
        backstage.io/kubernetes-id: cristal-web
    spec:
      nodeSelector:
        role: workload
      tolerations:
      - key: role
        operator: Equal
        value: workload
        effect: NoSchedule
      containers:
      - name: cristal-web
        image: harbor.harbor.svc.cluster.local/dxp/cristal-web:a45e237
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cristal-web
spec:
  selector:
    app: cristal-web
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cristal-web
  labels:
    app: cristal-web
    dxp.io/managed: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: cristal-web.68.221.5.247.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cristal-web
            port:
              number: 80
